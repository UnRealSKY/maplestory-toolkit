---
'maplestory-toolkit': patch
---

子母畫面不再依內容等比縮放，改成固定尺寸。之前視窗只有 200 高，三個王的內容都塞不下，只好把整個畫面縮到 0.58～0.74 倍——字縮到 8～11px、按鈕縮到 9px，換王時倍率還會跳，整個視窗忽大忽小。現在視窗開到 310 高（塞得下最高的阿卡），縮放固定 1，字級和按鈕都是原本的大小；換王、開打、血量變化，尺寸都不動。反盾的五顆按鈕固定 36px 高，剩餘的高度留給血條區塊；視窗拉窄到 420 以下時按鈕折成 3+2 兩排。
