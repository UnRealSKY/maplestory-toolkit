---
'maplestory-toolkit': patch
---

子母畫面改回「時間、血條、機制」三列直排，上一版把時間和血條併成一列是弄錯了。字和按鈕不會因此縮回去：兩條從來沒生效過的樣式一起修了（時間那列的輸入框、血條下方多出來的間距），三列直排的元素比 1.40.0 大約一成。另外面板高度改成釘在該王最高的那個狀態——之前按下「反盾開始」、或血量從等待變成有數字的那一刻，整個視窗的字會突然縮小一成多，現在待機時就先把空間留著，開打時不再跳動。
