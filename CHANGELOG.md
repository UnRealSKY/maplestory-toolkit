# 天天的楓之谷工具箱

## 1.41.3

### Patch Changes

- d766941: 沒在打王時不再冒出讀數。外框找不到就回 null，不再退回用顏色猜——舊的顏色爬行路徑會把遠征隊對話框裡那條橘色標題帶（只佔螢幕寬 25%）當成滿血、把天空當成藍色血條，子母畫面顯示 100%。那段舊路徑整個拆掉。

  拆掉之後補上一個原本被它掩蓋的問題：串流的 JPEG 色度降採樣會把鄰近血條的顏色染到 1px 的白線上（綠條上方變 246,255,203），判「白」改看亮度高、色度低，不再比整體 ΔE。

  15 張實戰截圖 × 9 種縮放全部合格，三張沒有王的畫面在每個尺寸都回 null。已知限制：1080p 的外框只有 1px，畫面縮到七成以下會糊掉找不到。

## 1.41.2

### Patch Changes

- 494eb74: 血條外框找錯線的兩個情況修掉。

  畫面上沒有王時（例如開著世界地圖）不再誤判：對話框的白邊佔螢幕寬 49%、正上方也比它暗，光看下邊線會被當成血條。擋下來靠的是「血量分界垂直對齊」——真血條每一列沒被遮住的分界都在同一個 x，被遮最多的那張還有 47% 的列合中位數；對話框上方是場景，23 列裡只有 2 列碰巧相近。

  血條正下方貼著「Boss 通知」橫幅時（凡雷恩），畫面縮到一半會讀錯：橫幅上緣也是一條白線、寬度跟血條一樣，縮小後暗色背景的雜訊被平均掉，那條線就跟右邊一個 12px 的白點橋接成 965px，比真底線的 771px 寬，「最寬的贏」選到它，97% 讀成 78%。改成候選線由上往下逐條算框，取第一條分界一致的——一條血條不可能把另一條底線包在裡面。整條同色的候選（滿血或空血）只在沒有別的候選時才採用。

  13 張實戰截圖在 1 到 0.47 共 9 種縮放比例下全部合格。

## 1.41.1

### Patch Changes

- 9596e0f: 血條判讀改成先找外框再讀框內。之前是從血色往左右爬，已損失區一變白或變成別的顏色就停在半路，19.8% 的血會讀成 48%。

  外框的下邊是固定三層結構：白線、上一列中灰內襯、再上一列暗色。只驗「白」會把場景裡的白色物件也收進來（實測讓下邊線多長 80px），加驗「白線正上方一定比它暗」之後就分得開了——真下邊線的亮度落差最低 11.1，純白場景是 0.0。下邊線被 UI 蓋掉一半時（只剩 47% 是白）靠「缺口上方是不是還是血條」把斷開的白段接回同一條線。

  框內讀哪幾列，用「血條的一列只有兩種顏色」來挑：蓋在血條上的面板會多出好幾群顏色，兩色比掉到 50~71%，乾淨的血條列是 88~100%。剩餘時間面板蓋住血條下半部時，原本會把面板左緣當成血量分界，讀成 16% 而不是 29%。

  分界點改成找最佳分割點（左邊盡量是剩餘色、右邊盡量是已損失色），不再數「第一群顏色連續到哪」——一個壓縮雜訊像素就會把 run 切斷，真分界在 x432 卻讀到 x394。

  八張實戰截圖的誤差全在 ±0.3% 內（原本系統性低估 1.2%），2K 完全準確。順帶修掉兩件事：血量低於 1.2% 時會讀成 0%，以及整條同色時分不出滿血與空血（王被打死那一刻會讀成 100%）。

## 1.41.0

### Minor Changes

- f72a7f9: 子母畫面改成每隻王有自己的大小。以前不管打哪隻王都是同一個視窗尺寸，反盾王塞不下要縮小、效率推估又空一大半；現在開起來與換王時都會調成剛好塞滿那隻王的尺寸，字級固定不再忽大忽小。你自己拉過視窗之後就不再自動調。

  同一批修掉的版面問題：各區塊改成固定高度，開打那一刻不會再整個視窗跳一下；反盾的五顆按鈕不再等寬平分，「反盾阻止成功」六個字終於有左右留白，「重置」也不再空蕩蕩；女皇每格裡的 −／＋ 不再貼在格線上；倒數的引信改成壓在卡片邊框上（看起來是那一段框變粗），不再是框內另畫一圈；只閃兩秒的魔消提示改成浮動顯示，不再為它常態空出一行。

### Patch Changes

- 14b7e7d: 「下一階段」的預告從「反盾開始（禁止輸出）」改成「反盾期間（禁止輸出）」——「反盾開始」是下面那顆按鈕的名字，寫在預告裡會讓人以為要去按它，這裡講的其實是接下來會進入的狀態。

## 1.40.7

### Patch Changes

- a719728: 女皇子母畫面的「觸發」按鈕調成跟反盾五顆按鈕一樣大（36px），不再一邊大一邊小。每格底下的 −1s／＋ 1s 在五格並排時擠出格子邊界，改成只留「−」「＋」符號，主視窗不變。

## 1.40.6

### Patch Changes

- 8f4437f: 子母畫面不再依內容等比縮放，改成固定尺寸。之前視窗只有 200 高，三個王的內容都塞不下，只好把整個畫面縮到 0.58 ～ 0.74 倍——字縮到 8 ～ 11px、按鈕縮到 9px，換王時倍率還會跳，整個視窗忽大忽小。現在視窗開到 310 高（塞得下最高的阿卡），縮放固定 1，字級和按鈕都是原本的大小；換王、開打、血量變化，尺寸都不動。反盾的五顆按鈕固定 36px 高，剩餘的高度留給血條區塊；視窗拉窄到 420 以下時按鈕折成 3+2 兩排。

## 1.40.5

### Patch Changes

- 53ca65f: 杜納斯、皮卡啾的子母畫面下方那一大片空白修掉了。原因是一條從來沒生效過的樣式：反盾那五顆按鈕在小視窗裡本該可以縮窄，卻被面板自己的「最少 120px」蓋掉，五顆加起來 600px 擠不進 480 寬的視窗只好換成兩行；子母畫面算縮放時先量到兩行的高度、撐開版面後又縮回一行，高度對不上就取矮的那個，底下空掉三成。修好後五顆按鈕排一行，480×200 的視窗剛好塞滿、不用縮放。按鈕的換行排版保留，視窗拉窄時照樣會折行。

## 1.40.4

### Patch Changes

- 9c45dbc: 2560×1440 的螢幕擷取血量時會一直「找不到血條」，截圖拿去分析卻都正常——差別在擷取的串流是壓縮過的。血條越粗、外框離內容越遠，2K 下外框正好落在程式往外搜尋的最後一格，串流壓縮讓上下界偏個 1px 就超出範圍。搜尋距離改成跟著血條高度放大，1080p 以下不受影響。

## 1.40.3

### Patch Changes

- 42a408b: 子母畫面改回「時間、血條、機制」三列直排，上一版把時間和血條併成一列是弄錯了。字和按鈕不會因此縮回去：兩條從來沒生效過的樣式一起修了（時間那列的輸入框、血條下方多出來的間距），三列直排的元素比 1.40.0 大約一成。另外面板高度改成釘在該王最高的那個狀態——之前按下「反盾開始」、或血量從等待變成有數字的那一刻，整個視窗的字會突然縮小一成多，現在待機時就先把空間留著，開打時不再跳動。

## 1.40.2

### Patch Changes

- 9c67309: 血量判讀修了兩個實戰才會踩到的問題。一、血量低的時候會抓不到血條：畫面上方那片粉紫色天空的彩度比剩一小截的血條還高，第一關就把背景當成血條。二、更糟的是右端會提早收手——血條上下緣的漸層讓整欄檢查在空槽區失敗，右端停在血色結束的地方，於是血量 18% 被報成 100%、85% 報成 98%，數字看起來正常，DPS 和門檻提醒全跟著錯。這次用實戰截下來的六張畫面逐一驗過，也把其中四張放進測試，之後再壞會直接被擋下。

## 1.40.1

### Patch Changes

- ef87bcc: 子母畫面裡的字和按鈕變回原本的大小。遊戲計時那一列本來就該跟血量並排，但它一直自己獨佔一整列，把內容多撐高 43px——子母畫面是等比縮放去塞滿那個小視窗的，內容越高就縮得越多，所有東西跟著變小。現在兩者併回同一列，元素放大約兩成，回到「補上遊戲計時」之前的大小。

## 1.40.0

### Minor Changes

- fbcf01f: 選了哪隻王現在會寫進網址，例如杜納斯是 `#/boss-toolkit/dunas`——這條連結可以直接貼給隊友或加書籤，開起來就是那隻王的計時器。不帶王的 `#/boss-toolkit` 一樣會回到你上次選的那隻。計時中仍然不能換王，直接改網址也會被擋回來，不會把跑到一半的計時弄丟。

  網址裡的王名改用 MapleStory 的英文名：皮卡啾／粉豆是 `pink-bean`、女皇是 `cygnus`、阿卡伊農是 `arkarium`、杜納斯還是 `dunas`。同時把程式裡把「反盾」誤稱為 shield 的地方全部正名為 damage reflect（`shield` 在楓之谷英文語境是玩家的護盾，意思剛好相反）。

  你存好的自訂秒數與上次選的王都會自動搬到新名字，不用重設；改版前的舊網址（`#/shield` 等）照樣會轉到新位置。

### Patch Changes

- dff9b3d: 網址打錯字、或分享出去的連結被截斷時，不會再開到空白頁。改版前那幾條列得出來的舊網址（`#/shield`、`#/pending` 等）本來就有轉址，但沒列到的（`#/abc`、`#/loot/abc` 這類）會停在一片空白上——上面那條導覽列還在，看起來像整個站壞掉。現在一律轉回 BOSS 工具箱。

## 1.39.1

### Patch Changes

- 8e04bbb: 修正「更新內容」裡的網址整個不見的問題（1.35.0 那條的新網址原本是空的）。

## 1.39.0

### Minor Changes

- c749701: 「更新內容」整份改寫成給使用者看的說法——原本每一條都在講實作細節，對要一起看更新的人沒有用。另外拿掉每個版本底下的 `Minor Changes` / `Patch Changes`，那是發版用的分級標籤，不是給看更新的人的資訊。

## 1.38.0

### Minor Changes

- 6f52ff0: 導覽列的站名、兩個工具箱、版本號現在對齊在同一條線上。

## 1.37.0

### Minor Changes

- 5f0efc6: 分寶工具箱底下的三個分頁改成對齊在「分寶工具箱」正下方。

## 1.36.0

### Minor Changes

- 1b68403: 分寶紀錄、未領總覽、設定移進上方的導覽列裡，比較看得出那幾個字是可以點的。

## 1.35.0

### Minor Changes

- a0a6884: 改名為「天天的楓之谷工具箱」，網址換成 [unrealsky.github.io/maplestory-toolkit](https://unrealsky.github.io/maplestory-toolkit/)。⚠ 舊網址的書籤要重設。

  上方導覽只剩兩個：**BOSS 工具箱**（原機制計算機）與**分寶工具箱**。分寶紀錄、未領總覽、設定收進分寶工具箱底下，右上的齒輪拿掉。開網址進來預設是 BOSS 工具箱。改版前的頁面網址會自動轉到新位置，存在瀏覽器裡的紀錄不受影響。

- f7c36bc: 整理資料在瀏覽器裡的存放名稱。開啟頁面時會自動搬移，你不用做任何事。

## 1.34.0

### Minor Changes

- e00d537: 改名為「打王工具箱」——原本的名字只講到分寶，但實際上用得最多的是機制計算機。

## 1.33.0

### Minor Changes

- d254afa: 修正王血量剩很少時讀不到血條的問題。
- e281569: 「從畫面同步」改成靠計時器本身的長相去認，不管它被拖到畫面哪個角落、遊戲視窗開多大都讀得到。

## 1.32.0

### Minor Changes

- 378f2c6: 新增「效率推估」：只有王血量，沒有機制面板，純粹用來看輸出。

  阿卡伊農過 20% 之後開始每 70 秒一輪的倒數，到點響鈴並自動接下一輪，可以 ±1 秒微調或重新計時。血量那一排的數字間距也調過，看得出哪個數字屬於哪個項目。

- 378f2c6: 開啟抬頭顯示後，主視窗那份面板會留著，不再被搬空——兩邊是各自一份，排版也可以不一樣（女皇在主視窗五格、在小視窗排成一列）。主視窗切到背景時計時也不會停。
- 378f2c6: 遊戲計時多一顆「從畫面同步」：直接讀遊戲畫面上的剩餘時間並完成對齊，等於幫你把 mm:ss 打進去再按對齊，照樣可以手動改。讀的是「王血量」已經在分享的那份畫面，不用再分享一次。

## 1.31.0

### Minor Changes

- 879cdf6: 王血量加上預估倒數（剩餘血量 ÷ 目前 DPS，多條血的王只算現在這一條），血量、預估、DPS 併成一排。

  另外修正沒在打王的地方也會冒出讀數的問題——畫面上任何長條色塊都會被當成血條，現在要真的有血條外框才算。

## 1.30.0

### Minor Changes

- ee113c1: 阿卡伊農加上「離上個門檻已過多久」，用來反推隊友技能的冷卻好了沒。換王或重置會清掉。
- ee113c1: 輸出速度改得更準：王回血不再倒扣，邊回血邊打時不會被低估。另外新增 60 秒內與整場的最高速度——機制打斷、跑位、王無敵讓當下速度歸零時，仍然看得出打得順的時候有多快。換階段或換王時血條會換顏色，不同顏色的資料不會混在一起算。

## 1.29.1

### Patch Changes

- d89c31e: 抬頭顯示裡的文字不會再被意外反白選取；輸入遊戲計時的欄位不受影響，照樣能編輯。

## 1.29.0

### Minor Changes

- e1072ad: 機制計算機新增「阿卡伊農」：這隻王不看時間看血量，掉到 80/60/40/20 就會出招。面板主角是距離下一個門檻還有幾 %，快到時轉成警戒色，跨過時響鈴並標記已過；一口氣掉很多時中間跨過的門檻也會全部標到。提前提醒的幅度可以自己調（預設 5%）。血量讀數跟「王血量」共用，所以要先開擷取。

### Patch Changes

- e1072ad: 王血量的每秒傷害量改成只要有讀數就顯示（沒掉血時顯示 0.0%）。原本沒掉血就整個不見，看起來像功能壞了。

## 1.28.4

### Patch Changes

- 93bd01a: 修正王血量固定讀成 100% 的問題。
- 93bd01a: 修正抬頭顯示的三個問題：女皇的遊戲計時愈按愈偏（輸入 44:00 卻顯示 44:23）、多冒出一顆重置按鈕、以及開啟後女皇的「接下來」時間表被搬走、主視窗變空的。

## 1.28.3

### Patch Changes

- eba4562: 修正遊戲視窗縮小時王血量讀成 100%（甚至完全讀不到）的問題。

## 1.28.2

### Patch Changes

- 5dddaac: 抬頭顯示補上遊戲計時對齊（輸入、對齊、±1 秒校準與跟著跑的時鐘），版面也整個壓緊，同樣的內容佔的高度少很多。另外修正女皇按下觸發後卡片突然變高、整排高低不齊的問題。

## 1.28.1

### Patch Changes

- 229f05c: 抬頭顯示改成 480×200，內容自動縮到剛好塞滿視窗、不會出現捲軸（換王、開始擷取或拉動視窗都會重算）。女皇在小視窗改排成一列，不然疊成好幾排會把字縮到看不清。

## 1.28.0

### Minor Changes

- 2c38ff2: 機制計算機新增「抬頭顯示」：把王血量與當前王的機制面板放進一個浮在最上層的小視窗，打王時遊戲在前景也看得到倒數與血量。反盾王連操作按鈕一起帶過去，女皇則是五個機制的觸發鈕；事件表與參數留在頁面。關掉小視窗內容會回到原位，計時不中斷。需要 Chrome 或 Edge，不支援的瀏覽器不會出現這顆按鈕。

## 1.27.2

### Patch Changes

- e5846bd: 王血量的版面調整：拿掉血量曲線，血條改成全寬、百分比移到血條下方，數字固定一位小數並鎖住字寬，血量跳動時版面不會跟著左右晃。

## 1.27.1

### Patch Changes

- e749548: 修正多條血的王算成滿血的問題——扣掉的部分露出來的是「下一條血」的顏色而不是空的，右邊那段亮色是底不是血。比例條也改成跟遊戲裡一樣：左邊是目前的血，右邊是露出來的下一條底色。

## 1.27.0

### Minor Changes

- e1d0af5: 機制計算機新增「王血量」：分享遊戲視窗後每秒判讀 BOSS 血條，顯示剩餘百分比、跟著血條實際顏色走的比例條，以及最近的掉血速度。血條顏色會變、多條血的王會有好幾種顏色、視窗大小一改位置與長度也跟著變，這些都能自動跟上；真的抓不到時可以在畫面快照上自己框選範圍（換視窗大小不用重框）。這一區跟王無關，任何王都能開。

## 1.26.0

### Minor Changes

- 5a92d03: 反盾面板改以「可輸出到」為主角：大字顯示可以打到幾分幾秒，本段剩餘秒數與 ±1 秒微調退成小字。時刻不會再每秒漂移。對齊遊戲計時後另外顯示持續走的當前遊戲計時，隨時可以跟遊戲畫面核對。
- 5a92d03: 「反盾計算機」更名為「機制計算機」。畫面與操作不變。
- 5a92d03: 機制計算機新增「女皇」：五個機制各自固定間隔（活屍 60 秒、鎖潛能 90 秒、變豬 60 秒、反盾 80 秒、小黑屋 90 秒），只算多久觸發一次，不管持續與魔消。每個機制各自按「觸發」開始數、可 ±1 秒微調，到點自動接下一輪並響鈴，寬螢幕一列擺得完五個。也一樣有遊戲計時對齊與「接下來」時間表，對齊點兩個面板共用。
- 5a92d03: 分寶紀錄列表每一列標出所屬的 DC 群組。

### Patch Changes

- 5a92d03: 倒數的引信邊框與進度條改成逐幀更新，不再一秒跳十格。

## 1.25.1

### Patch Changes

- e487996: 團員建議依群組分開：只取同一群的紀錄裡出現過的人，不再把別團的團員混進建議清單。

## 1.25.0

### Minor Changes

- 7ecc23b: 新增「物品出售」截圖區塊：整區所有照片合併成一則訊息。「掉落截圖」與「物品出售」都加上 Discord 每則 10 張附件的上限提示與發佈前檢查。

## 1.24.0

### Minor Changes

- e852af1: 反盾阻止成功時，倒數下方多一行小字「可輸出還有 N 秒」——阻止成功後接的那段間隔也是能打的，算進來才知道實際還有多久。間隔中已標記魔消時，同樣會把後面的阻止成功與下一段間隔算進去。

## 1.23.0

### Minor Changes

- 3a40db3: 團長辛苦費改為預設關閉——多數團只收 3% 手續費不收辛苦費。⚠ 升級後團長卡片會消失，有設辛苦費的紀錄金額也會跟著變（已發佈的貼文需要重新同步）。要繼續用的群組請到設定頁把「啟用團長辛苦費」打開。

## 1.22.0

### Minor Changes

- b4e390f: 反盾計算機加上 ±1 秒微調：大色塊倒數兩側各一顆按鈕，實況跟倒數差一兩秒時就地校正，不必整段重按。微調等於整條時間軸一起平移，事件表會跟著改，不會出現倒數改了但事件表沒跟上。

  未同步提醒改成只管當下正在編輯的那一筆。原本關閉分頁會掃全部紀錄，刻意留著不同步的場次（例如東西還在慢慢賣）每次都跳提醒。

## 1.21.0

### Minor Changes

- 90008a6: 分配加入交易手續費：編輯頁「基本資料」多一個欄位，新紀錄預設 3%。手續費與團長辛苦費並列，都是從團隊總額扣掉再均分，不是一層扣完再扣另一層。沒填的舊紀錄視為 0，既有金額完全不受影響。

  設定頁每個 DC 群組多一個「啟用團長辛苦費」開關。關掉之後編輯頁不出現「團長」卡片，分配與 DC 輸出也不算辛苦費。既有群組維持啟用，行為不變。

  DC 輸出的總共行改成把百分比留在算式裡，貼回來時讀得回百分比。舊格式的貼文仍然讀得懂。

## 1.20.0

### Minor Changes

- b402f22: 反盾計算機用詞調整：「反盾失敗」一律改為「反盾阻止成功」。

  魔消擴充三項：王對魔消有 80 秒耐性、技能本身 60 秒冷卻（都從施放當下起算），魔消持續改為預設 20 秒。耐性與冷卻只提醒不阻擋——按錯魔消成功要能改回來，所以耐性中照樣可以標記，只是不再閃爍催放、也不響提示音。

  新增「反盾間隔浮動」（皮卡 3 秒、杜納斯 0 秒，可調）：反盾重施實際會落在 間隔 ~ 間隔＋浮動 之間。它只影響魔消時機，該停手的時間點不變。

  上方大色塊加上引信效果：邊框繞著色塊隨倒數逐漸燒短。

## 1.19.1

### Patch Changes

- def0efa: 團員清單裡的團長那一列改為唯讀：換人或移除都在上方「團長」欄位做，兩處都能改會互相打架。

  修正全新使用者一開站名冊就被自動帶入的問題。⚠ 已經被帶入的設定不會自動回退，要清請到設定頁把名單切成「自行輸入」。

## 1.19.0

### Minor Changes

- 6b63554: 右上角顯示目前版本號，點一下開「更新內容」。內容是即時從線上抓的，所以文案更新不必等發版。離線或抓不到時會提示並附上連結，版本號本身不需要網路。

## 1.18.0

### Minor Changes

- b585355: 設定頁移回右上角的齒輪 ⚙，導覽列只留分寶紀錄／未領總覽／反盾計算機。另外加了一個隱藏捷徑：群組名稱取為「贖罪券」時，名單會自動接上官方名冊並立即抓取，不必自己貼網址。

## 1.17.0

### Minor Changes

- 1002538: 新增「設定」頁，把名單管理與 Webhook 設定合併進去，並擴充成多個 DC 群組。一個群組＝一套設定（名稱、Webhook、名單），每筆分寶紀錄綁定一個群組，發佈、同步、別名顯示與標註都跟著紀錄自己的群組走——帶不同公會的團不會互相干擾，同一個人在兩個伺服器有不同暱稱也各顯示各的。舊設定會自動變成第一個群組。綁著紀錄的群組不能刪除，至少要保留一個。

## 1.16.0

### Minor Changes

- 325198a: 新增團長辛苦費：編輯頁可指定一位團長並設定辛苦費（團隊總額的百分比或固定金額）。辛苦費先從總額扣掉，餘額均分給所有人，團長再額外拿走這筆。DC 輸出的總共行會把辛苦費寫進算式，貼回來時可以還原。
- 2aa53e2: 名冊改成四個欄位：Discord 帳號、Discord 顯示名、Discord 使用者 ID、自訂別名。Discord 那邊的名字跟自己取的別名分開存，同步只覆蓋 Discord 那三欄，別名永遠不會被蓋掉。顯示優先序是 別名 → Discord 顯示名 → Discord 帳號。舊資料會自動搬遷。

### Patch Changes

- 325198a: 修正代售漏算：代售是團員代替團隊賣掉的東西，收益屬於團隊，但先前沒有計入總額，那筆錢在分配時憑空消失（代售者被扣款，其他人卻沒分到）。改成「團隊總額＝總表淨額＋代售淨額」。⚠ 有代售的既有紀錄金額會變成正確的值，已發佈的貼文需要重新同步。

## 1.15.1

### Patch Changes

- 515e59c: 修正手機版排版：導覽列等短標籤不再被擠成一字一行；窄螢幕的導覽列改成獨占一行可橫捲，編輯頁工具列自動換行，表格改成超出時橫向捲動而不是把每一格壓扁。

## 1.15.0

### Minor Changes

- 9e89fcf: 反盾計算機支援多王：標題列新增王的選單（皮卡啾／粉豆、杜納斯），每隻王各自記住反盾持續與間隔的調整；魔消持續改成玩家技能的全域參數，預設 15 秒。計時中不能換王，避免拿到錯的倒數。

## 1.14.0

### Minor Changes

- 5fb32d0: 新增「反盾計算機」（皮卡啾／粉豆）：反盾／間隔／反盾失敗三態循環，倒數自動推進，看到遊戲內實況時可以用三顆按鈕隨時人工校準。魔消太早按會閃黃三下提示無效；魔消有效中色塊會閃爍；可輸出、魔消時機、禁止輸出都有聲音提醒（可關）。支援 mm:ss 遊戲計時對齊，各項秒數都可以調。

## 1.13.0

### Minor Changes

- 13c499d: DC 圖片全套：掉落／領錢／外購三類截圖，支援拖拉與 Ctrl+V 貼上、一鍵同步增刪、圖片網址過期自動刷新、刪除防護。新增名單管理頁（名冊與品名清單三種來源：預設／自訂網址／自行輸入，可匯出）。列表顯示 DC 同步徽章；未同步就離開或關分頁會提醒。

## 1.12.1

### Patch Changes

- 44ecd2d: 標題行與未領總覽的場次行拿掉「/ 人數」（干擾閱讀，人數看團員清單就知道，總共行也還有除數）。新舊格式都讀得懂。

## 1.12.0

### Minor Changes

- e29eae8: 打開編輯器就檢查跟 DC 貼文一不一致（✓ 一致／● 不同步／⚠ 貼文已失效），編輯中即時反映、同步後重新檢查。另外修正非劃線項目的備註沒被輸出而遺失的問題，貼回來時可以還原。

## 1.11.0

### Minor Changes

- e084913: 發佈至 DC 支援真的標註：名冊填了 Discord 使用者 ID 的人，發佈時 @帳號 會變成真的 mention（沒填 ID 的維持原樣）。同步時如果貼文或討論串已經被刪掉，會問你要不要重新發文；其他失敗不會誤導你重發。「綁定貼文」常駐，隨時可以換綁。

## 1.10.0

### Minor Changes

- d4b89cb: 內購新增「全額／均攤」模式：均攤的買家只付原價的 1/N，其他人分實付額，輸出時尾端標 (均攤)。品名整理：製作法家族統一全名（含百列 → 百烈）、珍珠手杖(+4AD) 併入龍之珍珠手杖且屬性移到註解。

## 1.9.0

### Minor Changes

- 0916013: DC Webhook 發佈／同步（第一版）：⚙ 貼上 URL 即時驗證，發佈成論壇貼文（標題 [MM-DD]團名）、同步更新內文、貼文掉了可以重新綁定。標題行改帶狀態標記（🛒 待售項數、💲 未結清人數，全結案 ☑）。未填金額輸出 ? 而不是 0，貼回來時可以還原。

## 1.8.0

### Minor Changes

- c0034a7: 紀錄可以標記「擱置」（暫不列入未領總覽，列表會顯示擱置徽章）；未領總覽每場新增「開啟 ↗」，開新分頁直達該紀錄的分配名單；紀錄狀態跨分頁即時同步。

## 1.7.0

### Minor Changes

- 5e50d49: 新增「未領總覽」頁：未結清款項依團員分區塊、可逐行複製、標記結清、標註待售。品名建議改成模糊比對，打一半或跳著打也找得到。

## 1.6.1

### Patch Changes

- 6b5e7f5: 列表改依日期新到舊排序；同日期再依團名，沒填日期的排最後。

## 1.6.0

### Minor Changes

- 28f5937: 列表新增兩個徽章：「全數賣出」（沒有待售項目）與「全部結清」（所有團員已結清）。修正品名下拉被表格裁切、選不到的問題。歷史金額改成純參考（滑上去看），選品名時不再自動填入單價。

## 1.5.0

### Minor Changes

- a1eaee7: 品名清單改成共用，更新清單不必等發版。「王名」欄位標籤改為「團名」。標記「不計入」時自動帶上註解「(價格過低不計入)」（已經有自訂註解就保留）。

## 1.4.0

### Minor Changes

- 0526ce8: 團員名冊與別名改成共用，更新名冊不必等發版。團員在畫面上（團員列、內購、代售、分配）顯示別名，貼回 DC 時仍然用原本的帳號，所以貼回來還是認得出來。建議選單同時包含共用名冊與本機歷史，下拉顯示「別名 (帳號)」。

### Patch Changes

- 887afaa: 總表新增項目時狀態預設「待售」、數量預設空白。修正品名與團名的建議比對。
- fccdce8: 狀態循環順序改為 待售 → 售出 → 不計入；不計入的圖標改用 ✖（舊格式的貼文仍然讀得懂）。

## 1.3.0

### Minor Changes

- 688f713: 代售支援剪刀成本：代售的物品若需消耗剪刀交易，剪刀成本從代售者持有額扣除（持有淨額＝單價 × 數量 − 剪刀單價 × 剪刀數），跟總表一樣。編輯卡片加上剪刀價與剪刀數欄位。

## 1.2.0

### Minor Changes

- 468c12c: 新增「代售」：記錄某團員代賣的物品／金額（代售者限團員），併入分配結算——結算＝收入 − 自己代售額，避免金幣二次轉手被遊戲扣手續費。有代售時分配面板會多出「代售」「結算」兩欄。

## 1.1.0

### Minor Changes

- 045ff8c: 分配金額改為無條件進位，避免因為四捨五入讓團員少拿。

## 1.0.1

### Patch Changes

- 4113fa7: 修正發版不會產生版本標籤的問題。

## 1.0.0

### Major Changes

- 9e83270: 首次正式發佈：DC 打王分寶 editor

  - 表單化編輯：總表（單價 × 數量、剪刀成本）、內購（限團員）、直播檔、即時分配面板
  - 分配公式：總表均分 ＋ 他人內購／(人數 − 1) − 自己內購，人數由團員數推導
  - 貼上／複製 DC 來回轉換，相容 emoji 與短碼、@名稱與數字 ID
  - 歷史建議（品名、團員、帶日期的單價、團名），多筆紀錄存在你自己的瀏覽器裡
